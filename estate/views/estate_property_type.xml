<odoo>
    <!--  List View -->
    <record id="estate_property_type_list_view" model="ir.ui.view">
        <field name="name">estate.property.type.list</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <list string="Estate Property View" >
                <field name="name"/>
                <field name="sequence"  widget="handle"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="estate_property_type_form" model="ir.ui.view">
        <field name="name">estate.property.type.form</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                    </group>

                    <notebook>
                        <page string="Properties">
                            <field name="property_ids">
                                <list editable="bottom">
                                    <field name="name"/>
                                    <field name="property_type_id" options="{'no create':True,'no_open':True}"/>
                                </list>
                            </field>
                        </page>
                    </notebook>

                </sheet>
            </form>
        </field>
    </record>

     <!-- Action for List view-->
    <record id="action_estate_property_type_list" model="ir.actions.act_window">
        <field name="name">List of Property Type</field>
        <field name="res_model">estate.property.type</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
